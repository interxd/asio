cmake_minimum_required(VERSION 3.5)

PROJECT (HELLOT)
SET(SRC_LIST main.c)
MESSAGE(STATUS "This is BINARY dir " ${RPOJECT_BINARY_DIR})
MESSAGE(STATUS "This is SOURCE dir " ${PROJECT_SOURCE_DIR})
ADD_EXECUTABLE(hello ${SRC_LIST})

